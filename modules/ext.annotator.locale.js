/**
 * Annotator Extension Locale Script
 * Author: Dominik Martin, BenjaminHosenfeld
 */

/*$.when( mw.loader.using( [ 'mediawiki.api.messages', 'mediawiki.jqueryMsg' ] ), $.ready )
	// Then: Load the messages that you need (if they are not yet loaded)
	.then( function() {
		return new mw.Api().loadMessagesIfMissing( [ 'helloworld', 'annotate-button-text', 'annotate-button-desc' ] );
	} );*/